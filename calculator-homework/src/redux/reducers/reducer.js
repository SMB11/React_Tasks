const initialState = {
    results: []
};

const reducer = (state = initialState, action) => {
    switch (action.type) {

        case "ADD":
            return {

            };
        case "SUBTRACT":
            return {

            };
        case "DIVIDE":
            return {

            };
        case "MULTIPLY":

            return {

            };
    }

    return state;
};
export default reducer;
